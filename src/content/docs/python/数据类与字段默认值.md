---
order: 106
title: 数据类与字段默认值
module: python
category: 'dev-lang'
difficulty: advanced
description: 'Python dataclass详解：字段默认值、不可变数据类。'
author: fanquanpp
updated: '2026-06-14'
related:
  - python/异步编程详解
  - python/类型注解与mypy
  - python/描述符
  - python/弱引用
prerequisites:
  - python/语法速查
---

## 1. 基本用法

```python
from dataclasses import dataclass, field

@dataclass
class User:
    name: str
    age: int = 0
    tags: list[str] = field(default_factory=list)

u1 = User("Alice", 30)
u2 = User("Alice", 30)
u1 == u2  # True（自动生成 __eq__）
```

## 2. field 配置

```python
@dataclass
class Config:
    name: str
    id: int = field(default_factory=lambda: uuid4().hex)
    internal: str = field(repr=False, compare=False)  # 不参与 repr 和比较
    _cache: dict = field(default_factory=dict, init=False)  # 不参与 __init__
```

## 3. 不可变数据类

```python
@dataclass(frozen=True)
class Point:
    x: float
    y: float

p = Point(1.0, 2.0)
p.x = 3.0  # FrozenInstanceError!
```

## 4. 继承

```python
@dataclass
class Animal:
    name: str

@dataclass
class Dog(Animal):
    breed: str

Dog("Rex", "Labrador")
```

注意：有默认值的字段不能在无默认值的字段之前。
