---
order: 105
title: 动态库与静态库
module: c
category: 'dev-lang'
difficulty: advanced
description: C语言动态库与静态库：.so/.a生成与链接。
author: fanquanpp
updated: '2026-06-14'
related:
  - c/二级指针与指针数组
  - c/函数指针回调与跳转表
  - c/volatile关键字
  - c/位域
prerequisites:
  - c/概述
---

## 1. 静态库（.a）

```bash
# 编译为目标文件
gcc -c mathlib.c -o mathlib.o

# 打包为静态库
ar rcs libmath.a mathlib.o

# 使用
gcc main.c -L. -lmath -o main
```

## 2. 动态库（.so）

```bash
# 编译为位置无关代码
gcc -fPIC -c mathlib.c -o mathlib.o

# 生成动态库
gcc -shared -o libmath.so mathlib.o

# 使用
gcc main.c -L. -lmath -o main

# 运行时需要设置库路径
export LD_LIBRARY_PATH=.:$LD_LIBRARY_PATH
```

## 3. 对比

| 特性     | 静态库                    | 动态库                     |
| -------- | ------------------------- | -------------------------- |
| 链接时机 | 编译时                    | 运行时                     |
| 文件大小 | 可执行文件大              | 可执行文件小               |
| 更新     | 需重新编译                | 替换 .so 即可              |
| 内存     | 每个进程一份              | 多进程共享                 |
| 扩展名   | .a (Linux) .lib (Windows) | .so (Linux) .dll (Windows) |
