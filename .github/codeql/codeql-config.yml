name: Custom CodeQL Configuration

# 仅分析 JavaScript/TypeScript 代码，禁止 CodeQL 自动检测其他语言
# （自动检测会触发 C++ autobuild 失败，因本项目无 C++ 代码）

paths-ignore:
  - '**/node_modules/**'
  - '**/dist/**'
  - '**/.astro/**'

queries:
  - uses: security-extended
  - uses: security-and-quality
